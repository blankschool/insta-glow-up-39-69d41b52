VITE_DEV_TEST_EMAIL="test@example.com"
VITE_DEV_TEST_PASSWORD="strong-password"
VITE_DEV_SEED_TEST_ACCOUNT="true"

# Optional: if set, `useInsights` can call `instagram-fetch-insights-dev` without Supabase login.
VITE_DEV_INSIGHTS_SECRET="dev-secret"

# DEV ONLY (no login): used only for display/selection; token stays server-side as Supabase secrets.
VITE_DEV_IG_USER_ID="17841462876000205"
VITE_DEV_IG_USERNAME="blankschoolbr"
